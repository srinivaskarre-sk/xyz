[build]
  command = "hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.128.0"
  NODE_VERSION = "20"
  # Use extended Hugo version for better features

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
